import express from 'express';
const router = express.Router();

// This file is for potential future user-related routes
// Currently kept for consistency with project structure

export default router;